export interface Theme {
  name: string;
  foreground: string;
  background: string;
  cursorColor: string;
  black: string;
  white: string;
  red: string;
  green: string;
  yellow: string;
  blue: string;
  purple: string;
  cyan: string;
}
