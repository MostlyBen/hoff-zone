// import { useRouter } from 'next/router';
import React from 'react';

const NotFoundPage = () => {
  // const router = useRouter();

  // React.useEffect(() => {
  //   router.replace('/');
  // });

  return <div>Ooops, looks like there was a problem on our end!</div>;
};

export default NotFoundPage;
